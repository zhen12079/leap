# LeapAI Step2 OCC配置详细网络架构分析

## 📋 配置环境

### 启动配置
```json
{
    "name": "bev test debug",
    "program": "/opt/conda/bin/torchrun",
    "env": {"LEAPAI_TASK_CONFIG": "projects/perception/configs/lpperception_current_hpa_step2_occ.py"},
    "args": ["--nproc_per_node=1", "--master_port=21212", "tools/main.py", "--config", "projects/perception/entry.py", "--state", "val", "--ckpt", "/path/to/step2.ckpt"]
}
```

### 任务配置
```python
# Step2 OCC专用配置
job_name = "lphpa_v3.0_static_occ"
enable_lidar = True
use_dynamic_outputs = True
dynamic_task = False          # 关闭动态任务
static_task = False          # 关闭静态任务
occ_task = True             # 启用占用任务
attr_task = False           # 关闭属性任务

# 时序配置
enable_dynamic_temporal = False   # 关闭动态时序
enable_static_temporal = False    # 关闭静态时序
enable_occ_temporal = False       # 关闭占用时序

# 训练配置
train_steps = 0                    # 无基础训练
finetune_steps = 40000            # 微调步数
max_steps = 40000                  # 总步数
batch_size = 8                     # 批次大小
```

## 🎯 与其他配置的关键区别

### Step1 vs Step2 OCC
```python
# Step1配置 (多任务联合训练)
dynamic_task = True
static_task = True
occ_task = False
train_steps = 125000
finetune_steps = 0

# Step2 OCC配置 (占用任务微调)
dynamic_task = False
static_task = False
occ_task = True
train_steps = 0
finetune_steps = 40000
```

### Step2 Static/Dynamic vs Step2 OCC
```python
# Step2 Static/Dynamic配置
occ_task = False
batch_size = 16

# Step2 OCC配置  
occ_task = True
batch_size = 8  # 更小的批次大小
```

## 🏗️ Step2 OCC专用网络结构

### 🎯 占用网络核心组件
```python
# 占用任务配置
OCC Task Configuration:
├── 任务类型: 3D占用网格分割
├── 输入数据: LiDAR点云 + 相机BEV特征
├── 输出格式: 3D体素分割 (H×W×D)
├── 类别数量: 12类占用语义
├── 体素分辨率: 0.2m × 0.2m × 0.2m
├── 感知范围: [0.0, -20.0, -1.0, 65.6, 20.0, 3.0]
└── 网格尺寸: [328, 200, 20]
```

### 🧠 冻结策略 (关键优化)
```python
freeze_module_schedules = {
    "stage1_backbone0": train_steps,    # 冻结ResNet34 Group1
    "stage1_backbone1": train_steps,    # 冻结ResNet34 Group2
    "stage1_backbone2": train_steps,    # 冻结ResNet34 Group3
    "stage1_backbone3": train_steps,    # 冻结ResNet34 Group4
    "stage1_neck0": train_steps,        # 冻结FPN Neck Group1
    "stage1_neck1": train_steps,        # 冻结FPN Neck Group2
    "stage1_neck2": train_steps,        # 冻结FPN Neck Group3
    "stage1_neck3": train_steps,        # 冻结FPN Neck Group4
    "view_transfer": train_steps,        # 冻结BEV变换
    "lidar_vfe": train_steps,           # 冻结LiDAR特征提取
    "lidar_middle_encoder": train_steps,  # 冻结LiDAR中间编码器
    "lidar_bev_backbone": train_steps,   # 冻结LiDAR BEV骨干
    "fuser": train_steps,               # 冻结多模态融合器
}
```

### 🎯 可训练模块 (仅训练这些)
```python
trainable_modules = [
    "at_seg_neck",           # 占用颈部网络
    "at_seg_head",           # 占用分割头
]
```

## 📊 数据流动与Shape变化

### 占用网络数据流
```
Step 1: 输入数据预处理
├── 相机图像: [B, 7, 3, 512, 960] → 150MB
├── LiDAR点云: 变长 → 体素化 → 61MB
├── 标注数据: 3D占用网格标签
├── 点云范围: [0.0, -20.0, -1.0, 65.6, 20.0, 3.0]
└── 体素尺寸: [0.2, 0.2, 0.2]

Step 2: 特征提取 (冻结)
├── 相机特征: 4×ResNet34V2 → [B, 7, 256, H/8, W/8]
├── FPN颈部: 多尺度融合 → [B, 7, 256, H/8, W/8]
├── BEV变换: IPM + 可变形注意力 → [B, 256, 112, 140]
├── LiDAR特征: VFE + 中间编码器 → [B, 256, 112, 140]
└── 多模态融合: 相机+LiDAR → [B, 256, 112, 140]

Step 3: 占用区域裁剪
├── 输入特征: [B, 256, 112, 140]
├── OCC区域: [0.0, -20.0, -1.0, 65.6, 20.0, 3.0]
├── 空间特征裁剪: [B, 256, 200, 328] (H×W)
├── LiDAR特征裁剪: [B, 256, 50, 82] (H/4×W/4)
└── 特征对齐: 确保空间一致性

Step 4: 占用颈部处理 (可训练)
├── SegNeckLite: 轻量化分割颈部
├── 输入: 空间特征 [B, 256, 200, 328] + LiDAR特征 [B, 256, 50, 82]
├── 下采样路径:
│   ├── Down1: [B, 256, 200, 328] → [B, 128, 200, 328]
│   ├── Down2: [B, 128, 200, 328] → [B, 128, 200, 328]
│   └── Down3: [B, 128, 200, 328] → [B, 256, 200, 328]
├── LiDAR特征处理: [B, 256, 50, 82] → [B, 128, 50, 82]
├── 特征融合: [B, 256, 200, 328] + [B, 128, 50, 82] → [B, 384, 200, 328]
├── 上采样路径:
│   ├── Up1: [B, 384, 200, 328] → [B, 128, 200, 328]
│   ├── Up2: [B, 128, 200, 328] → [B, 128, 200, 328]
│   └── Up3: [B, 128, 200, 328] → [B, 64, 200, 328]
└── 输出特征: [B, 64, 200, 328]

Step 5: 占用分割头 (可训练)
├── 输入特征: [B, 64, 200, 328]
├── 分割卷积: [B, 64, 200, 328] → [B, 240, 200, 328]
├── 维度重排: [B, 240, 200, 328] → [B, 20, 12, 200, 328]
├── 最终输出: [B, 12, 20, 200, 328] (C×D×H×W)
└── 语义分割: 12类占用语义
```

### 内存占用分析
```python
# 基础内存 (与Step1相同)
Base Memory: ~4.3GB per GPU
├── 输入数据: ~211MB
├── 特征提取: ~3.9GB
└── 基础输出: ~150MB

# 占用网络额外内存
OCC Network Memory: ~800MB per GPU
├── 3D体素网格: 20×200×328×4bytes ≈ 5MB
├── 中间特征图: 64×200×328×4bytes ≈ 17MB
├── 上采样特征: 128×200×328×4bytes ≈ 34MB
├── 分割输出: 12×20×200×328×4bytes ≈ 63MB
├── 梯度内存: ~200MB
├── 优化器状态: ~400MB
└── 临时计算: ~100MB

# 总内存占用
Total Memory: ~5.1GB per GPU
```

## 🎯 网络输出详细说明

### 占用分割输出 (3D语义分割)
```python
# 基础占用输出
Occupancy Segmentation: [B, 12, 20, 200, 328]
├── Batch: B (批次大小)
├── Classes: 12 (语义类别)
├── Depth: 20 (高度层数)
├── Height: 200 (BEV高度)
├── Width: 328 (BEV宽度)
├── Resolution: 0.2m × 0.2m × 0.2m
├── Coverage: 65.6m × 40m × 4m
└── Format: 语义概率分布

# 语义类别定义
Semantic Classes:
├── 0: Void (其他/无效区域)
├── 1: Road (路面)
├── 2: Sidewalk (人行道)
├── 3: Road-edge (路边缘)
├── 4: Vehicle (车辆: car, truck, bus)
├── 5: VRU (弱势道路使用者: ped, rider, non-motor)
├── 6: Vegetation (植被)
├── 7: Ghost (鬼影)
├── 8: ParkingBarrier (停车障碍物)
├── 9: GMO (动态障碍物)
├── 10: GSO (静态障碍物: fence, barrier, pillar, pole)
└── 11: Tree (树木)

# 原始类别映射 (30类 → 12类)
Original Classes (30 → 12):
├── 0,15,17,20,22 → 0: Void
├── 1,2,4,29 → 1: Road
├── 3 → 2: Sidewalk
├── 5 → 3: Road-edge
├── 7,8,9 → 4: Vehicle
├── 10,11,13 → 5: VRU
├── 19 → 6: Vegetation
├── 21 → 7: Ghost
├── 28 → 8: ParkingBarrier
├── 24 → 9: GMO
├── 6,16,18,25,26,27 → 10: GSO
└── 30 → 11: Tree
```

### 损失函数输出
```python
# 占用损失函数
Occupancy Loss Components:
├── CrossEntropy Loss: 标准交叉熵损失
├── Lovasz-Softmax Loss: Lovász softmax损失
├── Ignore Index: 255 (忽略无效标签)
├── Class Weight: None (无类别权重)
└── Loss Weight: 1.0

# 损失计算
Loss Calculation:
├── CE Loss: 标准语义分割损失
├── Lovasz Loss: 优化IoU的损失函数
├── Total Loss: CE + Lovasz
└── Purpose: 提升边界分割精度
```

## 🚀 推理与部署

### 验证模式配置
```python
# 验证时的特殊处理
state = "val"
eval_with_visualize = True  # 启用可视化
eval_instance = False       # 关闭实例评估

# 批次大小
batch_sizes = {
    "occ": {"train": 8, "val": 1},  # 验证时batch_size=1
}
```

### 可视化输出
```python
# BEV可视化配置
draw_occ_cfg = dict(
    occ_conf=0.5,                 # 占用置信度
    draw_3d_grid=True,           # 绘制3D网格
    draw_semantic=True,            # 绘制语义信息
    valid_range=[0.0, 65.6, -20.0, 20.0],  # 可视化范围
    voxel_size=[0.2, 0.2, 0.2],  # 体素尺寸
    grid_size=[328, 200, 20],     # 网格尺寸
    max_height=4.0,               # 最大显示高度
    color_map={                    # 颜色映射
        0: [0, 0, 0],      # Void: 黑色
        1: [128, 128, 128],  # Road: 灰色
        2: [255, 255, 0],    # Sidewalk: 黄色
        3: [255, 165, 0],    # Road-edge: 橙色
        4: [255, 0, 0],      # Vehicle: 红色
        5: [0, 255, 0],      # VRU: 绿色
        6: [0, 255, 255],    # Vegetation: 青色
        7: [255, 0, 255],    # Ghost: 洋红
        8: [128, 0, 128],    # ParkingBarrier: 紫色
        9: [255, 255, 255],  # GMO: 白色
        10: [0, 0, 255],     # GSO: 蓝色
        11: [0, 128, 0],      # Tree: 深绿色
    }
)

# 3D可视化
occ_3d_visualization = {
    "show_voxels": True,          # 显示体素
    "show_point_cloud": True,      # 显示点云
    "show_semantic": True,        # 显示语义
    "transparency": 0.7,          # 透明度
    "point_size": 2.0,            # 点大小
    "voxel_alpha": 0.5,          # 体素透明度
}
```

## 📈 性能优化策略

### 内存优化
```python
# 冻结策略
freeze_modules = [
    "stage1_backbone*",      # 冻结所有骨干网络
    "stage1_neck*",         # 冻结所有颈部网络
    "view_transfer",        # 冻结视图变换
    "lidar_*",              # 冻结LiDAR模块
    "fuser",               # 冻结融合器
]

# 可训练模块
trainable_modules = [
    "at_seg_neck",           # 占用颈部网络
    "at_seg_head",           # 占用分割头
]
```

### 计算优化
```python
# 混合精度
use_backbone_amp = True  # 骨干网络AMP

# 梯度累积
accumulate_grad_batches = 1

# 学习率缩放
group_lr_scale = {
    "stage1_backbone": 1.0,  # 冻结，实际不更新
    "stage1_neck": 1.0,       # 冻结，实际不更新
}

# 批次大小优化
batch_size_optimization = {
    "train": 8,    # 训练批次大小
    "val": 1,      # 验证批次大小
    "reason": "3D占用网络内存消耗大，使用较小批次"
}
```

## 🎯 关键技术特点

### 占用网络优势
1. **3D感知**: 真正的3D占用网格表示
2. **多模态融合**: 相机BEV特征 + LiDAR点云
3. **语义分割**: 12类精细语义理解
4. **高分辨率**: 0.2m体素分辨率
5. **大范围**: 65.6m × 40m × 4m感知范围

### 专注占用优化
1. **任务专注**: 关闭动态静态任务，专注占用分割
2. **参数高效**: 冻结大部分网络，只训练关键模块
3. **轻量化设计**: SegNeckLite减少计算量
4. **内存优化**: 较小批次大小控制内存使用

### 网络架构特点
```python
# SegNeckLite架构特点
SegNeckLite Architecture:
├── 轻量化设计: 减少通道数和层数
├── 多尺度融合: 下采样 + 上采样路径
├── 特征对齐: 空间特征与LiDAR特征对齐
├── 渐进式解码: 逐步恢复分辨率
└── 跳跃连接: 保留细节信息

# 3D分割头特点
3D Segmentation Head:
├── 高度维度: 20层高度信息
├── 语义维度: 12类语义输出
├── 卷积设计: 1×1卷积降维
├── 维度重组: H×W×C → D×C×H×W
└── 概率输出: 每个体素的语义概率
```

## 📊 数据增强策略

### LiDAR数据增强
```python
# 训练时数据增强
Lidar Augmentation:
├── 点云旋转: Z轴±30°, Y轴±5°
├── 随机翻转: X轴镜像
├── 位置偏移: [±0.15m, ±0.15m, ±0.15m]
├── 点云打乱: 随机重排点顺序
├── 范围掩码: 裁剪到有效范围
└── 体素化: 规则化点云表示

# 验证时处理
Validation Processing:
├── 位置偏移: [1.75m, 0m, 1.587m]
├── 范围掩码: 裁剪到感知范围
├── 体素化: 统一网格表示
└── 标签对齐: 确保空间一致性
```

## 📊 评估指标

### 占用分割指标
```python
# 评估指标配置
Occupancy Metrics:
├── mIoU: 平均交并比
├── Class IoU: 每类IoU
├── Pixel Accuracy: 像素准确率
├── Class Accuracy: 每类准确率
└── Frequency Weighted IoU: 频率加权IoU

# 评估范围
Evaluation Range:
├── X轴: [0.0m, 65.6m]
├── Y轴: [-20.0m, 20.0m]
├── Z轴: [-1.0m, 3.0m]
├── 体素尺寸: 0.2m × 0.2m × 0.2m
└── 网格尺寸: 328 × 200 × 20
```

这个Step2 OCC配置代表了占用分割任务的专门优化阶段，通过3D语义分割网络实现了高精度的环境占用感知。与Step2 Static和Step2 Dynamic配置形成完整的多任务感知体系，分别专注于静态、动态和占用三个不同的感知维度。
